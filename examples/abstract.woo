// abstract.woo - abstract methods (unimplemented) for abstract classes / interfaces

class Shape {
    // abstract method: declared without a body
    float area()

    // concrete method calling the abstract one (dynamic dispatch)
    void describe() {
        sys.println("area = " + sys.float2str(this.area()))
    }
}

class Rect extends Shape {
    float w = 0.0
    float h = 0.0

    init(float w, float h) {
        this.w = w
        this.h = h
    }

    float area() {
        return this.w * this.h
    }
}

Rect r = Rect(3.0, 4.0)
r.describe()
sys.println(sys.is(Shape, r))



class BB {
    void m() {
        sys.println("BB.m")
    }
}


class VCV{
    BB bb

    int chen(){
        return 3/7
    }
}

// VCV() auto-runs init() if present, and the BB bb field auto-constructs -> fully initialized
VCV().bb.m()